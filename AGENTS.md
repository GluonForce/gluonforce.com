# AGENTS.md

## Cursor Cloud specific instructions

This repository (`gluonforce.com`) is currently an empty/scaffold project with only a `README.md`. There are:

- **No application services** to run
- **No dependencies** to install
- **No build steps, lint checks, or tests** configured
- **No package manager lockfiles** or dependency manifests

Future agents should re-evaluate this file once application code is added. At that point, update this section with service startup commands, testing instructions, and any non-obvious development caveats.
